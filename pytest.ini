[pytest]
testpaths = tests
markers =
    slow: test is slow (renders PDFs / spawns subprocesses)
filterwarnings =
    # Ignore deprecation warning from httplib2 (external dependency)
    ignore::DeprecationWarning:httplib2